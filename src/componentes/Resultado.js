import React, {Fragment} from 'react';

const Resultado = ({resultado}) => {
    return (  
        <Fragment>
            <p>{resultado} mililitros de pHplus</p>
        </Fragment>
    );
}
 
export default Resultado;